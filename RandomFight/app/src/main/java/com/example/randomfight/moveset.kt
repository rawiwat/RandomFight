package com.example.randomfight

class moveset {
    fun attack (attackerSpeed:Int, dodgerSpeed:Int, attacker:Int, defensePoint:Int, ) : Int {
        TODO( "will output whether or not the attack hit and how much damage was dealt maybe also how much health was left")
        //use speed to calc if it hit or not
        //if it does it use attack point and defense point to calc damage
        //output dmg
    }
}